spring.application.name=course-record
server.servlet.context-path=/course-record

# Pagination for list APIs (query params: page, size, sort). Controllers also use @PageableDefault(size=20).
spring.data.web.pageable.default-page-size=20
spring.data.web.pageable.max-page-size=100
spring.data.web.pageable.page-parameter=page
spring.data.web.pageable.size-parameter=size

spring.config.import=optional:file:./config/application-local.properties

spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true

spring.liquibase.change-log=classpath:liquibase/mysql/db-changelog-master.xml

# Managed MySQL (e.g. DigitalOcean) often sets sql_require_primary_key=ON; Liquibase's
# DATABASECHANGELOG DDL has no PK until newer Liquibase versions. Relax for the session only.
spring.datasource.hikari.connection-init-sql=SET SESSION sql_require_primary_key=0

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# jwt.secret: set JWT_SECRET (e.g. App Platform / docker compose) or config/application-local.properties for local runs.
jwt.expiration-ms=3600000

app.admin.username=admin
app.admin.password=admin

# CORS for browser clients (e.g. Next.js). Comma-separated origins; required when allowCredentials=true (no "*").
app.cors.allowed-origins=http://localhost:3000
